frappe.listview_settings["Farm Task Execution"] = {
	// Filter tabs and options come from DocType status field (Ready, In Progress, ...).
	// Add default filters, formatters, or onload here if needed.
};
